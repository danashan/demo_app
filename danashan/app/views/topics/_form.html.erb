<!--/app/views/topics/_form.html.erb> -->
 <%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title, :size => 60 %>
  </div>
  <div class="field">
    <%= f.label :forum_Title %>
	<%= f.collection_select :forum, Forum.all, :id, :title%>
  </div>
  <div class="field">

	<%= f.label :user_id %>
	<%= f.collection_select :user, User.all, :id, :name%>
  </div>

  <div class="actions">
    <%= f.submit :as => 'Create Topic' %>
  </div>
<% end %> 



